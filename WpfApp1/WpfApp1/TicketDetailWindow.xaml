<Window x:Class="WpfApp1.TicketDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chi tiết phản ánh"
        Height="420" Width="650"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0 0 0 10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Tiêu đề:"
                       Grid.Row="0" Grid.Column="0"
                       Margin="0 0 8 8"
                       VerticalAlignment="Center"/>
            <TextBox x:Name="txtTitle"
                     Grid.Row="0" Grid.Column="1"
                     Margin="0 0 0 8"/>

            <TextBlock Text="Nội dung:"
                       Grid.Row="1" Grid.Column="0"
                       Margin="0 0 8 8"
                       VerticalAlignment="Top"/>
            <TextBox x:Name="txtContent"
                     Grid.Row="1" Grid.Column="1"
                     Margin="0 0 0 8"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     Height="120"
                     VerticalScrollBarVisibility="Auto"/>

            <TextBlock Text="Trạng thái:"
                       Grid.Row="3" Grid.Column="0"
                       Margin="0 0 8 8"
                       VerticalAlignment="Center"/>
            <ComboBox x:Name="cboStatus"
                      Grid.Row="3" Grid.Column="1"
                      Margin="0 0 0 8"
                      Width="180"/>

            <TextBlock Text="Gán cho:"
                       Grid.Row="4" Grid.Column="0"
                       Margin="0 0 8 0"
                       VerticalAlignment="Center"/>
            <ComboBox x:Name="cboAssigned"
                      Grid.Row="4" Grid.Column="1"
                      Width="220"
                      DisplayMemberPath="Display"
                      SelectedValuePath="TaiKhoanID"/>
        </Grid>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Lưu"
                    Width="90" Margin="0 0 8 0"
                    Click="BtnSave_Click"/>
            <Button Content="Đóng"
                    Width="90"
                    Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>
